# Local .terraform directories
**/.terraform/*

# Terraform state files (may contain secrets / account details)
*.tfstate
*.tfstate.*
crash.log
crash.*.log
errored.tfstate

# Sensitive variable overrides (keep committed values only)
*.auto.tfvars

# CLI config
.terraformrc
terraform.rc

# macOS
.DS_Store
